# 🚀 Деплой на Netlify за 2 шага

> **Важно**: Для работы на Netlify нужен отдельный бэкенд. Используй Beget, VPS или любой другой хостинг для Python-приложения.

---

## Шаг 1: Подготовка бэкенда

### 1.1 Разверни бэкенд на Beget/VPS
Следуй инструкции [`DEPLOY_BEGET_SIMPLE.md`](DEPLOY_BEGET_SIMPLE.md) или используй свой VPS.

### 1.2 Получи URL бэкенда
Например: `https://username.beget.tech`

---

## Шаг 2: Деплой фронтенда на Netlify

### 2.1 Настрой API URL
В локальном проекте создай файл `frontend/.env.production`:

```env
VITE_API_URL=https://username.beget.tech
```

*Замени `username.beget.tech` на твой реальный URL бэкенда.*

### 2.2 Собери фронтенд
```bash
cd frontend
npm install
npm run build
```

### 2.3 Загрузи на Netlify

**Вариант А: Drag & Drop (самый простой)**
1. Зайди на [app.netlify.com](https://app.netlify.com/)
2. Войди в аккаунт
3. Перетащи папку `frontend/dist` в окно "Deploy manually"
4. Готово! Получишь URL вида `https://your-project-name.netlify.app`

**Вариант Б: Через Git**
1. Залей проект на GitHub
2. В Netlify нажми "Add new site" → "Import an existing project"
3. Выбери репозиторий
4. Настрой:
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
5. Нажми "Deploy site"

---

## ✅ Готово!

Теперь открой URL Netlify в браузере. Фронтенд будет общаться с бэкендом на Beget/VPS.

---

## 🔧 Настройка CORS

Убедись, что в `.env` на бэкенде указан правильный `FRONTEND_URL`:

```env
FRONTEND_URL=https://your-project-name.netlify.app
```

Перезапусти бэкенд после изменения.

---

## 📝 Важно

- **Фронтенд на Netlify** - только интерфейс
- **Бэкенд на Beget/VPS** - API и боты
- **Токен и admin_id** настраиваются в интерфейсе фронтенда
- **CORS** должен быть настроен правильно для связи фронтенда и бэкенда

---

## 🔄 Обновление проекта

### Обновление фронтенда
```bash
cd frontend
npm run build
```
Затем перезалей папку `dist` на Netlify.

### Обновление бэкенда
Загрузи новые файлы на Beget/VPS и перезапусти Python-приложение.