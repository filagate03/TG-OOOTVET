# 🚀 Деплой на VPS (Ubuntu) за 2 шага

## Шаг 1: Загрузка файлов на сервер

### 1.1 Подключение к серверу через SSH
Открой терминал (PowerShell, CMD или VS Code) и подключись:

```bash
ssh root@IP-СЕРВЕРА
```

*Замените `IP-СЕРВЕРА` на IP вашего VPS.*

### 1.2 Загрузка файлов
**Вариант А: Через SFTP (рекомендуется)**
1. Используй FileZilla или WinSCP
2. Подключись к `IP-СЕРВЕРА` с данными root
3. Перейди в папку `/tg-otvet` (создай если нет)
4. Загрузи ВСЕ файлы проекта, КРОМЕ:
   - `.venv/`
   - `frontend/node_modules/`
   - `frontend/dist/`
   - `__pycache__/`
   - `*.pyc`

**Вариант Б: Через Git**
```bash
cd /tg-otvet
git clone https://github.com/your-repo.git .
```

---

## Шаг 2: Автоматическая установка и запуск

### 2.1 Перейди в папку проекта
```bash
cd /tg-otvet
```

### 2.2 Сделай скрипт исполняемым и запусти
```bash
chmod +x start_all.sh
./start_all.sh
```

**Скрипт автоматически:**
- ✅ Установит системные зависимости
- ✅ Создаст виртуальное окружение
- ✅ Установит все Python зависимости
- ✅ Установит все Node.js зависимости
- ✅ Соберёт фронтенд
- ✅ Инициализирует базу данных
- ✅ Создаст файл .env
- ✅ Запустит бэкенд и ботов

### 2.3 Настрой автозапуска через systemd (опционально)

Чтобы система запускалась автоматически при перезагрузке сервера:

```bash
# Скопируй файл сервиса
cp tg-otvet.service /etc/systemd/system/

# Перезагрузи systemd
systemctl daemon-reload

# Включи автозапуск
systemctl enable tg-otvet

# Запусти сервис
systemctl start tg-otvet

# Проверь статус
systemctl status tg-otvet
```

**Полезные команды:**
- `systemctl stop tg-otvet` - остановить
- `systemctl restart tg-otvet` - перезапустить
- `systemctl logs tg-otvet` - посмотреть логи

---

## ✅ Готово!

Теперь открой в браузере: `http://IP-СЕРВЕРА:8002`

Ты увидишь панель управления. Создай проект, добавь токен бота и admin_id - бот запустится автоматически!

---

## 🔧 Если что-то не работает

### Проверь логи
```bash
tail -f ~/logs/python.log
```

### Перезапусти приложение
В панели Beget → Python → нажми **"Перезапустить"**

### Проверь, что фронтенд собран
```bash
ls -la frontend/dist/
```
Должен быть файл `index.html`

### Проверь базу данных
```bash
ls -la bot.db
```
Файл должен существовать

---

## 📝 Важно

- **Фронтенд встроен в бэкенд** - не нужно отдельно деплоить на Netlify
- **Боты запускаются автоматически** при добавлении проекта в базу
- **Токен и admin_id настраиваются в интерфейсе**, не в .env
- **Все файлы загружаются в public_html**